# 1. Квадрат числа
square = lambda x: x**2
# 2. Проверка на пустоту строки
is_empty = lambda s: len(s) == 0
# 3. Склеивание строк
full_name = lambda first, last: f"{first} {last}"
# 4. Налог от суммы (10%)
get_tax = lambda price: price * 0.1
# 5. Условная лямбда (if/else)
check_val = lambda x: "Много" if x > 10 else "Мало"
